{
  "name": "mastra-agent",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "mastra dev",
    "build": "mastra build --deployer cloudflare",
    "deploy": "pnpm run build && wrangler deploy --config .mastra/output/wrangler.json"
  },
  "dependencies": {
    "@mastra/core": "latest",
    "@mastra/deployer-cloudflare": "latest",
    "@ai-sdk/openai": "latest",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "mastra": "latest",
    "typescript": "^5.2.2",
    "wrangler": "^4.0.0"
  }
}
